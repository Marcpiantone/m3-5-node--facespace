<li>
    <div class = "friend">
        <% const friend = users.find((user) => {%>
            <% return user._id === friendId });%>
                <a href="<%-`http://localhost:8000/users/${friend._id} `%>"><img src="<%-friend.avatarUrl %>" alt="<%-friend.name %> picture"></a>
            <p><%- `${friend.name}`%></p>
    </div>
    </li>